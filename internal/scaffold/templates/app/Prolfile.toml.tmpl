# This template documents the Prolfile.toml structure generated by `prolm new`.
# Actual generation uses manifest.Save() for deterministic serialization.
#
# [meta]
# min_prolm_version = "0.1.0"
# prolfile_version = 1
#
# [package]
# entry = "src/main.pl"
# name = "{{.Name}}"
# runtime = "{{.Runtime}}"
# version = "0.1.0"
